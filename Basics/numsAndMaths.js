const score = 40000;
console.log(score.toLocaleString());

//const x = (1, 2, 3, 5, 9);

console.log(Math.min(10, 3, 6, 9));
console.log(Math.max(45, 67, 0, 100));

//Use Math.random() to generate nunber between 0 and 1
console.log(Math.round(Math.random() * 10));
